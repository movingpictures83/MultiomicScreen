# MultiomicScreen
# Language: Python
# Input: CSV 
# Output: CSV
# Tested with: PluMA 1.1, Python 3.6
# Dependency:

PluMA plugin that takes a matrix of correlations
and screens for those that are between heterogeneous
data (assuming multiomics)

Input matrix is a CSV file, as is the (Screened) output matrix.
